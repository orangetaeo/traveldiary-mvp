# 회의록: 사이클 5a — Railway 데모 모드 배포

**날짜**: 2026-04-29
**참석**: R1·R7·T15·T16·T13
**ADR**: ADR-016

## 합의

### A1. 코드 산출물 (이 사이클에서 작성)
- `package.json` scripts: `postinstall`, `db:migrate`, `db:deploy`, `db:studio` 추가 (db 명령은 사이클 5b 위해 미리 정의)
- `package.json` engines: Node 20+ 명시
- `railway.json`: nixpacks 빌드 + 헬스체크 + 시작 명령
- `app/api/health/route.ts`: 데모 모드 인식 헬스체크
- `next.config.js`: standalone 출력 검토 (Railway 빌드 캐시 활용용 — 일단 미적용)
- `.env.example` 갱신: 사이클 5b 추가 변수 자리 표시
- `next.config.js` 트레일링 슬래시·이미지 도메인 등 점검

### A2. 사용자 액션 가이드 (별도 안내)
- `git init` 후 첫 커밋 — 사용자에게 정확한 명령 제공
- GitHub 저장소 생성 + push
- Railway 가입 + 프로젝트 생성 + GitHub 연결
- 도메인 발급 + 배포 검증

### A3. 검증 통과 기준
- [ ] tsc 0 / next build 0 에러
- [ ] `npm start`로 로컬 production 부팅 (포트 3000)
- [ ] `curl http://localhost:3000/api/health` → 200 + `{ status: "demo" }`
- [ ] Railway 배포 후 외부 URL에서 M1~M4 골든패스 동작
- [ ] 시드니 사이트와 URL/프로젝트 분리 확인 (docs/07 §10)

### A4. 보안 (T16)
- 사이클 5a엔 비밀키 0개 — 배포 시 환경변수 빈 칸 OK
- 사이클 5b ADR에 API 키 등록 절차 명시 (Railway Variables, .env.example sync)

## 사인오프
R1 ✅ R7 ✅ T15 ✅ T16 ✅ T13 ✅
