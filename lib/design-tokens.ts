/**
 * TRAVELDIARY 디자인 시스템 — 단일 진실 공급원
 *
 * 이 파일이 우리 앱의 시각 언어를 정의합니다.
 * tailwind.config.ts와 동기화 유지.
 *
 * 룰:
 * 1. 색은 의미를 동반해야 한다. 예쁘다고 색을 추가하지 않음.
 * 2. 빨강은 진짜 위험에만 (알레르기, 환불 불가). 그 외 사용 금지.
 * 3. 한 화면에 색 3개 이내 — 메인 1 + 보조 1 + 회색.
 */

// ═══════════════════════════════════════════════════════════
// COLORS — 5색 + 회색 시스템
// ═══════════════════════════════════════════════════════════

export const colors = {
  ink: {
    DEFAULT: "#1A1F26",
    soft: "#5A6270",
    mute: "#8B8F98",
  },

  /**
   * 보라 — 계획·정보·AI 추천
   * 사용처: 추천 근거 패널, 예약 완료 표시, 일정 카테고리 "관광"
   */
  purple: {
    DEFAULT: "#5B4BC4",
    soft: "#ECE9FB",
    deep: "#3C3489",
  },

  /**
   * 코랄 — 진행·시간·여행 중
   * 사용처: 여행 중 헤더, 펄스 점, 현재 일정 강조, "강행" 옵션
   */
  accent: {
    DEFAULT: "#FF6B47",
    soft: "#FFE8E0",
    deep: "#993C1D",
  },

  /**
   * 앰버 — 주의·지연·사회적 증거
   * 사용처: 웨이팅 시간, 지연, "한국인 BEST", 카테고리 "맛집"
   */
  amber: {
    DEFAULT: "#C97A1F",
    soft: "#FAEEDA",
    deep: "#854F0B",
  },

  /**
   * 빨강 — 안전·알레르기 (절제해서 사용)
   * 사용처: 알레르기 함유, 환불 불가. 그 외 금지.
   */
  danger: {
    DEFAULT: "#C73F3F",
    soft: "#FCEBEB",
    deep: "#791F1F",
  },

  /**
   * 초록 — 안전·완료·여행 후
   * 사용처: 예약 완료 알림, "안전" 옵션, 카테고리 "휴식"
   */
  success: {
    DEFAULT: "#1D7F5C",
    soft: "#E1F5EC",
    deep: "#085041",
  },

  surface: {
    card: "#FFFFFF",
    soft: "#F4F1EB",
    dark: "#0F1419",
  },

  divider: "#E5E2DC",
} as const;

// ═══════════════════════════════════════════════════════════
// TYPOGRAPHY — 5단계만, 더 늘리지 않음
// ═══════════════════════════════════════════════════════════

export const typography = {
  /** 화면 헤더 (최상위) — "13:42", "도쿄 4박 5일" */
  h1: { size: 22, weight: 500 },

  /** 카드 메인 제목 — "스시 다이", "teamLab Planets" */
  h2: { size: 17, weight: 500 },

  /** 항목 제목, 본문 */
  body: { size: 14, weight: 400 },
  bodyBold: { size: 14, weight: 500 },

  /** 보조 정보, 메타 — "네이버 후기 92%", 부제 */
  caption: { size: 12, weight: 400 },

  /** 라벨, 캡션 — 시간, 거리, 작은 메타 */
  micro: { size: 11, weight: 400 },
} as const;

// ═══════════════════════════════════════════════════════════
// SPACING — 정보 우선순위는 여백으로 표현
// ═══════════════════════════════════════════════════════════

export const spacing = {
  /** 같은 의미 단위 안 (제목과 부제) */
  xxs: 4,

  /** 같은 그룹 안 (버튼 사이) */
  xs: 8,

  /** 카드 안의 섹션 사이 */
  sm: 12,

  /** 카드 사이 */
  md: 16,

  /** 큰 섹션 사이 (헤더 ↔ 본문) */
  lg: 24,
} as const;

// ═══════════════════════════════════════════════════════════
// RADIUS
// ═══════════════════════════════════════════════════════════

export const radius = {
  md: 8,
  lg: 12,
  pill: 999,
} as const;

// ═══════════════════════════════════════════════════════════
// CARD VARIANTS — 3가지 카드 위계
// ═══════════════════════════════════════════════════════════

export const cardVariants = {
  /** 일반 카드 — 회색 배경, 보조 정보 */
  plain: "bg-surface-soft",

  /** 강조 카드 — 흰색 배경 + 0.5px 회색 테두리, 주요 단위 */
  raised: "bg-surface-card border border-divider",

  /** 추천 카드 — 흰색 배경 + 2px 보라 테두리, 한 화면 1개만 */
  featured: "bg-surface-card border-2 border-purple",
} as const;
